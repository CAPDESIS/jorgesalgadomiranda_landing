# Jorge landing — ola 2026-09-11

Hostinger / static. **Not** capdesis.com. **No** `--force`. **No** deploy.

Local `main` at start: `5820f050e4ab21d18617fead729ac09cfe2f1994`.

## Scope

| Track | Decision |
|---|---|
| Legal hex vs CSS tokens | **KEEP chrome**, switch to site stylesheet |
| 404 | Same tokens, root-relative `/assets/styles.css`, no telemetry |
| i18n | **KEEP** stacked EN+ES on legal/404; catalog parity still gated |
| CI | `lint` + `build` + `test`; Node 24 on every workflow |
| Secrets | `api/secrets.php` stays gitignored; Web3Forms gate removed |

## Git (this ola)

- `9bc232f` `fix: legal and 404 consume site stylesheet tokens`

Changed:

- `legal/privacy.html`, `legal/terms.html`, `404.html`, `assets/styles.css`
- `package.json` (`lint` / `build` = `python3 scripts/validate-static-site.py`)
- `.github/workflows/{ci,test,deploy,gitleaks,release-policy}.yml`
- `scripts/validate-static-site.py` (Resend `/api/contact.php`, not Web3Forms)
- tests: `tests/aux-surfaces.test.ts`, `tests/ci-hygiene.test.ts`

## Live ≠ git

README URL: `https://jorgesalgadomiranda.com/`.

| Probe (2026-09-11, this machine) | Result |
|---|---|
| `GET /` | **429** Too Many Requests (Cloudflare). HTML SHA **UNKNOWN**. |
| `GET /legal/privacy.html` | **429**. Cannot compare hex vs stylesheet on apex. |
| `GET /404.html` | **429**. |
| `GET /robots.txt` | **200**, 3722 bytes, `cf-cache-status: HIT`. Body starts with Cloudflare managed Content-Signal block, then the repo policy. Git `robots.txt` has no CF managed prefix. |

Do not claim live equals this commit. Hostinger FTP was not run.

## Analytics

No new vendor. Index still has the existing Umami + Cloudflare + PostHog placeholders (`YOUR_*`). 404 does not load any of them. Do not add a fourth.

## Validation (local)

```text
bun run lint     # exit 0  Static site validation OK
bun run build    # exit 0  Static site validation OK
bun test --coverage
# 111 pass, 0 fail
# lines 100.00%  funcs 98.81%  (bun test --coverage, tests/ excluded)
php scripts/test-contact-api.php  # all contract tests passed
```

PHP printed `curl_close()` deprecation on 8.5. Pre-existing. Not this ola.

## Residual

- Apex still serves the previous Hostinger tree until a manual `workflow_dispatch` with an exact `origin/main` SHA.
- `RESEND_API_KEY` presence on the host is **UNKNOWN** from this session (no secrets read).
- Umami host in CSP remains `stats.capdesis.com` as the existing self-hosted collector for this site. That is not a capdesis.com marketing deploy.
