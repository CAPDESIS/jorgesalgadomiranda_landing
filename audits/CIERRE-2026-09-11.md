# JorgeLANDING - cierre 2026-09-11

Un escritor. Hostinger / static. **Not** capdesis.com. **No** `--force`. **No** FTP.

## Git hygiene

```text
git fetch origin
git branch --show-current          # main
git worktree list                  # one worktree: this repo
git worktree prune -v              # nothing MISSING
git merge-base --is-ancestor origin/main HEAD   # yes
```

| Ref | SHA |
|---|---|
| `origin/main` at fetch | `5820f050e4ab21d18617fead729ac09cfe2f1994` |
| local `HEAD` before this ledger | `71b6508a28369dcbc6d0f7a84f4de7f51cb373cd` |
| merge-base | `5820f05` = `origin/main` (fast-forward only) |
| worktrees | 1 (this path). prune: empty |

Ola commits not yet on `origin/main` at fetch:

- `9bc232f` `fix: legal and 404 consume site stylesheet tokens`
- `ab07589` `docs: record 2026-09-11 landing ola ledger`
- `71b6508` `docs: pin both 2026-09-11 ola SHAs in the ledger`

## KEEP (git)

| Track | Git evidence | Decision |
|---|---|---|
| Legales CSS vs hex | `legal/privacy.html` and `legal/terms.html` `href="/assets/styles.css"`, `data-surface="aux"`, `body.legal-doc`. `--ink: #` **absent**. Chrome KEEP (serif titles, 720px, EN+ES stacked). | **KEEP** |
| 404 | `404.html` same stylesheet, `body.page-404`, no `stats.capdesis.com`, no analytics loaders. Root-relative href for ErrorDocument. | **KEEP** |
| Build | `package.json` `lint` and `build` = `python3 scripts/validate-static-site.py`. Not Astro/Vite. | **KEEP** |
| CI Node24 | `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true` in ci, test, deploy, gitleaks, release-policy. | **KEEP** |
| Secretos | `.gitignore` has `api/secrets.php`. Validator rejects Web3Forms, requires `/api/contact.php`. No fourth analytics vendor. | **KEEP** |

No extra product code in this cierre. Implementation landed in `9bc232f`.

## Validation (this cierre)

```text
bun run lint     # exit 0  Static site validation OK
bun run build    # exit 0  Static site validation OK
bun test --coverage
# 112 pass / 0 fail
# lines 100.00%  funcs 98.81%  (bun test --coverage, tests/ excluded)
```

## Live ≠ git (not P0)

README URL: `https://jorgesalgadomiranda.com/`.

Probes 2026-09-11, this machine, HTTP 200 (Cloudflare). Live HTML cache-bust: `assets/styles.css?v=108043e` = git `108043e` `chore(deps): bump bun a 1.3.14 en CI... (#30)`. Hostinger is behind local `71b6508`. Jorge FTP / manual `workflow_dispatch`. **Not P0.**

| Path | HTTP | Live bytes | Git bytes | Live | Git |
|---|---|---|---|---|---|
| `/` | 200 | 182675 | 181159 | `styles.css?v=108043e`, `YOUR_UMAMI` still placeholder, `/api/contact.php` present | HEAD `71b6508` |
| `/legal/privacy.html` | 200 | 17143 | 12447 | `--ink: #` **yes**, no site stylesheet | stylesheet, no hex ink |
| `/legal/terms.html` | 200 | 12550 | 8496 | `--ink: #` **yes**, no site stylesheet | stylesheet, no hex ink |
| `/404.html` | 200 | 10364 | 3091 | inline tokens, `stats.capdesis.com` comment | `/assets/styles.css`, no capdesis.com |
| `/robots.txt` | 200 | 3722 | 1886 | Cloudflare Content-Signal prefix | repo policy only |
| `/assets/styles.css` | 200 | 81264 | 88276 | older sheet (no aux legal/404 block) | includes `body.legal-doc` / `body.page-404` |

`robots.txt` live ≠ git is Cloudflare managed overlay, not a Hostinger file miss.

## Residual (Jorge)

1. Promote `origin/main` after this push via existing Hostinger FTPS `workflow_dispatch` (exact SHA). Not done here.
2. `RESEND_API_KEY` on the host: **UNKNOWN** (no secrets read).
3. Umami CSP origin `stats.capdesis.com` is the existing collector for this site, not a capdesis.com marketing deploy.

## Push

Attempted ancestor `git push origin main` (no `--force`). GitHub GH013 rejected:

`Required status check "bun test --coverage (>= 85% lines)" is expected.`

`test.yml` runs on `pull_request`, not on push to `main`, so a direct main push cannot produce that check.

Same commits pushed as fast-forward branch `docs/cierre-jorge-landing-2026-09-11`. PR: https://github.com/CAPDESIS/jorgesalgadomiranda_landing/pull/43

Cierre commits: `9ca43e1` ledger, `a22e4cf` hyphen pin.
