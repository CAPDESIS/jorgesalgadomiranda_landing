[allowlist]
description = "Allowlist for known safe patterns"
paths = [
  '''(^|/)node_modules(/|$)''',
  '''(^|/)\.git(/|$)''',
  '''(^|/)vendor(/|$)''',
  '''(^|/)\.dart_tool(/|$)''',
  '''(^|/)build(/|$)''',
  '''(^|/)dist(/|$)''',
]
